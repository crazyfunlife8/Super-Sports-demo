# 博弈報表展示頁面 — 開發規格書

**版本**：v2.0
**日期**：2026-04-26
**用途**：本文件作為開發方與業主之間的功能與技術規範，供報價、驗收及開發執行使用。

---

## 目錄

1. [專案概述](#一專案概述)
2. [技術選型](#二技術選型)
3. [資料架構](#三資料架構)
4. [頁面範圍與功能清單](#四頁面範圍與功能清單)
5. [互動範圍定義](#五互動範圍定義)
6. [外部 API 整合規格](#六外部-api-整合規格)
7. [Supabase 雲端服務規格](#七supabase-雲端服務規格)
8. [瀏覽器與裝置支援](#八瀏覽器與裝置支援)
9. [排除項目](#九排除項目)
10. [交付物清單](#十交付物清單)
11. [驗收標準](#十一驗收標準)
12. [工期估算](#十二工期估算)
13. [授權與聲明](#十三授權與聲明)
14. [附錄](#附錄)

---

## 一、專案概述

### 1.1 專案目標

開發一套**博弈代理後台報表展示系統**，供業主作為教學、示範用途。系統外觀與操作流程 1:1 還原參考站報表頁面，業主透過「編輯模式」（URL hash 控制）直接在前台 inline 編輯所有示範資料，無獨立後台介面。

### 1.2 用途聲明

本系統為**展示／教學用途**，不連接任何真實博弈平台，不執行實際資金結算、不做風控、不做金流處理。所有資料由業主自行生成或輸入，僅用於示範畫面效果與講解博弈後台架構。

### 1.3 架構核心概念：三頁脫鉤

本系統採「三頁完全脫鉤」架構，三個前台頁面資料來源與邏輯完全獨立：

| 前台頁面 | 資料來源 | 業主操作方式 |
|---|---|---|
| 查詢報表頁 | Supabase `sites` 表（業主手填） | 編輯模式中 inline 點任一欄位改值，新增/刪除網站列 |
| 即時注單頁 | Supabase `tickets_aggregate` 表 + `matches` 表 | 編輯模式中 inline 點任一注單彙總值改，或用全域按鈕「批量隨機填」 |
| 歷史賽事頁 | Supabase `matches` 表 + `tickets_aggregate` 表 | API 每日同步賽事；注單彙總同即時注單頁編輯方式 |

三頁資料互不影響，業主可獨立維護每一頁的內容。

### 1.4 帳號視角定義

本案還原畫面為最高代理（大總監）開出之查帳子帳號視角。此視角可檢視底下所有代理層級的完整業績報表，為資料可視範圍最大的前台顯示層級。不含上層管理功能（開立子帳號、調整成數、設定層級額度等）。

> 注意：「代理視角」僅反映在**欄位標題**（會員結果、代理結果、總代結果…等 7 個欄位名稱），欄位數值由業主手動填入。

---

## 二、技術選型

| 項目 | 選擇 | 理由 |
|---|---|---|
| 前端框架 | HTML5 + Bootstrap 5 + jQuery 3（CDN 載入） | 與參考站技術棧一致；無 build step，降低交付與維護成本 |
| 後端伺服器 | 無 | 展示用途不需要，降低營運成本與複雜度 |
| 資料儲存 | Supabase（免費方案） | PostgreSQL 結構清晰、內建 Realtime 同步、開源可自行搬家 |
| 即時同步 | Supabase Realtime | 後台操作後，前台瀏覽器無需手動刷新即可看到更新 |
| 編輯模式控制 | URL hash（`?edit=true`）| 業主自己在書籤存編輯版網址；客戶端開純網址完全無法觸發任何編輯動作；無需帳號密碼登入機制 |
| RWD 響應式 | 不做 | 參考站為桌機設計（最低寬度 1170px） |
| 部署方式 | 靜態檔案（Netlify / GitHub Pages / 任意 Apache 主機） | 無伺服器需求，最低成本 |
| 賽事資料源 | 國際體育賠率聚合 API（首選 SportsGameOdds，備案 The Odds API） | 業主要求真實賽事與盤口；覆蓋 MLB / NPB / CPBL 三球種；實作前須驗證免費額度與 CPBL 覆蓋；若免費方案不符需求，備案付費方案（USD $50～200/月）|

---

## 三、資料架構

### 3.1 資料表總覽

共三張資料表，各自服務不同前台頁面：

| 資料表 | 服務頁面 | 資料來源 |
|---|---|---|
| `sites` | 查詢報表頁 | 業主編輯模式中 inline 填入 |
| `tickets_aggregate` | 即時注單頁、歷史賽事頁 | 業主編輯模式中 inline 填入彙總值，或用全域按鈕批量隨機填 |
| `matches` | 即時注單頁、歷史賽事頁 | API 每日自動同步 |

### 3.2 sites 表（查詢報表頁用）

此表每一列代表業主在查詢報表頁要顯示的一列「網站業績」資料，全部由業主手動填入。

| 欄位名稱 | 類型 | 說明 | 業主填入 |
|---|---|---|---|
| id | UUID | 系統自動產生 | ❌ |
| created_at | timestamp | 建立時間，系統自動 | ❌ |
| site_name | text | 網站名稱（對應前台第一欄「網站」） | ✅ |
| bet_count | integer | 筆數 | ✅ |
| bet_amount | numeric | 下注金額 | ✅ |
| valid_bet | numeric | 有效投注 | ✅ |
| pending_amount | numeric | 未過帳金額 | ✅ |
| member_result | numeric | 會員結果（正負數） | ✅ |
| agent_result | numeric | 代理結果（正負數） | ✅ |
| super_agent_result | numeric | 總代結果（正負數） | ✅ |
| shareholder_result | numeric | 股東結果（正負數） | ✅ |
| big_shareholder_result | numeric | 大股東結果（正負數） | ✅ |
| director_result | numeric | 總監結果（正負數） | ✅ |
| big_director_result | numeric | 大總監結果（正負數） | ✅ |
| remark | text | 備註（可空白） | ✅ |
| rate_scheme | text | 成數（可空白，如 0/95/95/95/95/97/97） | ✅ |
| display_date_from | date | 前台顯示用的查詢起始日期 | ✅ |
| display_date_to | date | 前台顯示用的查詢結束日期 | ✅ |

**合計列計算規則：**

- `bet_count`、`bet_amount`、`valid_bet`、`pending_amount`、`member_result`、`agent_result`、`super_agent_result`、`shareholder_result`、`big_shareholder_result`、`director_result`、`big_director_result` 欄位：前端自動加總所有列的數值
- `remark`、`rate_scheme`：合計列留空，不加總
- 合計列不儲存於資料庫，由前端即時計算

**日期欄位說明：**

`display_date_from` 與 `display_date_to` 是業主在編輯模式中填入的「這筆資料對應的查詢日期範圍」。業主在前台直接點擊日期欄位即可修改（純檢視模式與編輯模式皆可改），修改後即時存回 Supabase。日期變更不影響表格中其他欄位的數值，僅更新日期顯示。

### 3.3 tickets_aggregate 表（即時注單頁、歷史賽事頁用）

此表儲存業主建立的「賽事盤口彙總值」（每場賽事每個盤口種類的注單數與累計金額），對應即時注單頁與歷史賽事頁的注單顯示。**不儲存單筆注單細節**——這是 Demo 簡化設計，業主在編輯模式中直接 inline 填入彙總值。

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | bigserial | 主鍵 |
| `match_id` | text | 賽事編號（FK matches.match_id） |
| `bet_type` | text | 盤口種類：`handicap`（讓分）/ `total`（大小盤）/ `moneyline`（獨贏）/ `odd_even`（單雙）/ `first_inning`（一輸二贏） |
| `bet_position` | text | 下注方向：`home` / `away` / `over` / `under` / `odd` / `even` 等（依 bet_type 而定） |
| `count` | integer | 該方向累計注單數 |
| `total_amount` | numeric(12,2) | 該方向累計下注金額 |
| `created_at` | timestamptz | 建立時間 |
| `updated_at` | timestamptz | 最後修改時間 |

**約束：** `(match_id, bet_type, bet_position)` 唯一鍵。

**資料量估算：** 每場賽事 5 種盤口 × 平均 2 個方向 = 約 10 筆記錄。3 球種每日約 30 場賽事 = 約 300 筆/日。


### 3.4 matches 表（API 賽事同步用）

| 欄位 | 說明 |
|---|---|
| id | 系統識別碼 |
| sport | 球種：baseball_mlb / baseball_cpbl / baseball_npb |
| home_team | 主隊名稱（從 API 取得） |
| away_team | 客隊名稱（從 API 取得） |
| commence_time | 開賽時間（UTC，從 API 取得） |
| spread | 讓分盤口（從 API 取得，建議選 Pinnacle 或 API 預設首家） |
| totals | 大小盤口（從 API 取得） |
| moneyline | 獨贏賠率（從 API 取得） |
| status | 賽事狀態：upcoming / live / completed |
| synced_at | 最後一次從 API 同步的時間戳記 |

---

## 四、頁面範圍與功能清單

### 4.1 前台頁面總覽

| 頁面 | 檔案 | 說明 |
|---|---|---|
| 查詢報表頁 | index.html | 業主手填網站業績列的彙總報表，單頁無下鑽 |
| 即時注單頁 | live-bets.html | 即時賽事盤口監控頁，美棒、台棒、日棒各場賽事盤口彙總 |
| 歷史賽事頁 | history.html | 歷史賽事彙總，美棒、台棒、日棒 |

---

#### 頁面一：查詢報表頁（index.html）

##### 通用 Header（所有前台頁面共用）

| 元素 | 說明 |
|---|---|
| 左上 Logo 區 | 業主自訂品牌名稱（編輯模式可直接點改），不沿用原站商標 |
| 左上「問題回應」按鈕 | 純外觀 |
| 右上帳號資訊 | 顯示「您好，mx5144 [子帳號]」；帳號名稱與角色文字編輯模式中可直接點改 |
| 右上剩餘額度 | 業主自訂顯示數字（編輯模式可直接點改） |
| 右上重設密碼 | 純外觀，不接邏輯 |
| 右上登出按鈕 | 純外觀，不接邏輯 |
| 右上「編輯模式中」標示 | 僅編輯模式顯示，提示業主目前狀態 |
| 跑馬燈公告 | 自動向左滾動；編輯模式可點跑馬燈彈出文字編輯框 |
| 主導覽列 | 管理 / 即時注單 / 歷史賽事 / 查詢報表 / 在線會員 / Super彩 / 說明書 |
| 子選單麵包屑 | 總帳 ＞ RG真人（純文字，無功能） |
| 右下角全域動作按鈕區 | 僅編輯模式顯示（匯出/匯入/同步/批量填/清空/重置） |

##### 篩選列（兩排，純外觀除日期欄位外）

**第一排：**

| 元件 | 說明 | 互動狀態 |
|---|---|---|
| 帳務日期起 | 日期選擇器 | ✅ 前台可直接點改，修改後即時存回 Supabase |
| 帳務日期終 | 日期選擇器 | ✅ 同上 |
| 前一日按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 今日按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 後一日按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 本週按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 上週按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 本月按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 上月按鈕 | 快捷日期 | ✅ 自動填入日期欄位 |
| 報表分類下拉 | 純外觀 | ❌ 點擊無效果 |
| 投注種類下拉 | 純外觀 | ❌ 點擊無效果 |

**第二排：**

| 元件 | 說明 | 互動狀態 |
|---|---|---|
| 球種下拉 | 純外觀 | ❌ 點擊無效果 |
| 賽程時段下拉 | 純外觀 | ❌ 點擊無效果 |
| 整數下拉（數字格式） | 純外觀 | ❌ 點擊無效果 |
| 帳號輸入框 | 純外觀 | ❌ 可輸入但不觸發篩選 |
| 單號輸入框 | 純外觀 | ❌ 可輸入但不觸發篩選 |
| 查詢按鈕 | 純外觀 | ❌ 點擊無效果 |

> **重要**：日期欄位與 7 個快捷按鈕是本頁唯一有功能的篩選元件。日期修改後只更新畫面上顯示的日期數字，**不影響表格中任何其他欄位的數值**。表格資料是業主在後台建立的固定資料，日期僅作演示用途。

##### 標題列

| 元素 | 說明 | 互動狀態 |
|---|---|---|
| 「總帳」標題文字 | 固定顯示 | — |
| 上一頁按鈕 | 純外觀（本頁無下鑽，無上一頁概念） | ❌ |
| 刷新按鈕 | 重讀 Supabase sites 表 | ✅ 點擊重讀資料 |

##### 主表格

**表頭：**「全部」標籤（橘底，色碼 `#e28743`）＋ 14 個欄位標題

**14 欄位說明：**

| 欄位 | 對應 sites 表欄位 | 顯示規則 |
|---|---|---|
| 網站 | site_name | 純文字，第一欄（取代原站「總監」） |
| 筆數 | bet_count | 數字 |
| 下注金額 | bet_amount | 藍色文字（視覺保留，點擊無效果，無下鑽） |
| 有效投注 | valid_bet | 數字 |
| 未過帳金額 | pending_amount | 數字 |
| 會員結果 | member_result | 正值顯示綠色（`green` / `#23ba1e`）；負值顯示紅色（`#dc3545`） |
| 代理結果 | agent_result | 同上 |
| 總代結果 | super_agent_result | 同上 |
| 股東結果 | shareholder_result | 同上 |
| 大股東結果 | big_shareholder_result | 同上 |
| 總監結果 | director_result | 同上 |
| 大總監結果 | big_director_result | 同上 |
| 備註 | remark | 純文字 |
| 成數 | rate_scheme | 純文字 |

**合計列（最底列）：**

- 位置：固定於表格最底部
- 數字欄自動加總：筆數、下注金額、有效投注、未過帳金額、會員結果、代理結果、總代結果、股東結果、大股東結果、總監結果、大總監結果
- 備註欄：留空
- 成數欄：留空
- 合計列資料由前端即時計算，不儲存於 Supabase

---

#### 頁面二：即時注單頁（live-bets.html）

即時賽事盤口監控頁。每場賽事顯示為一行，欄位列出該賽事各盤口種類目前累計的注單數與金額，供管理者監控用。

**球種範圍：** 美棒（MLB）、台棒（CPBL）、日棒（NPB）共 3 種。其餘 18 種球類入口圖示外觀還原，點進去顯示「暫無賽事」。

**入口下拉：** 滑鼠移上 header「即時注單」展開 21 球種選單（外觀完整還原）。點美棒/台棒/日棒切換到對應球種頁。

**盤口階段頁籤（6 個）：**

| 頁籤 | 狀態 | 說明 |
|---|---|---|
| 早餐 | 外觀還原，無功能 | 開賽前早盤投注時段 |
| 單式 | **有功能** | 一般單場投注；本案僅此頁籤有實際資料 |
| 走地 | 外觀還原，無功能 | 開賽中即時投注 |
| 3 局 | 外觀還原，無功能 | 棒球特定局數投注 |
| 7 局 | 外觀還原，無功能 | 棒球特定局數投注 |
| 已開賽 | 外觀還原，無功能 | 已開賽賽事 |

點擊無功能頁籤顯示空白或「暫無資料」提示。

**自動刷新機制（1:1 還原原站）：**

- 頁面頂部顯示 11 秒倒數計時器
- 倒數歸零後自動從 Supabase 重讀資料更新畫面
- 提供「立即更新」按鈕供手動觸發
- 每次刷新只呼叫 Supabase，不呼叫外部 API

**篩選列互動：**

| 元件 | 互動 |
|---|---|
| 11 秒倒數 + 立即更新 | ✅ 有功能 |
| 全場下拉 | ❌ 純外觀 |
| 第 1 頁下拉 | ❌ 純外觀 |
| 注單種類（實際下注）下拉 | ❌ 純外觀 |
| 15 秒下拉 | ❌ 純外觀 |
| 排序（聯盟）下拉 | ❌ 純外觀 |

**表格欄位（每場賽事 1 行，單盤口主盤）：**

| 欄位 | 顯示內容 | 資料源 |
|---|---|---|
| 編號 | 賽事編號（8 位數字） | API matches.match_id |
| 時間 | 賽事開賽時間（MM-DD HH:MM） | API matches.game_time |
| 場次/隊伍 | 客隊名稱換行主隊名稱[主] | API matches.away_team / home_team |
| 讓分 | 讓分值 + 主家賠率/客家賠率 + 主家注單數/累計金額 / 客家注單數/累計金額 | 賠率 API + 注單彙總（業主後台填） |
| 大小盤 | 大小值 + 大盤賠率/小盤賠率 + 大盤注單數/累計金額 / 小盤注單數/累計金額 | 同上 |
| 獨贏 | 主家賠率/客家賠率 + 主家注單數/累計金額 / 客家注單數/累計金額 | 同上 |
| 單雙 | 單賠率/雙賠率 + 單注單數/累計金額 / 雙注單數/累計金額 | 同上 |
| 一輸二贏 | 值 + 主賠率/客賠率 + 主注單數/累計金額 / 客注單數/累計金額 | 同上 |

**資料源分層：**
- 賽事資訊（編號、時間、隊伍）：API matches 表（每日同步）
- 5 種盤口賠率值：API matches 表（每日同步）
- 注單數/累計金額：Supabase tickets_aggregate 表（業主後台填，前台 11 秒讀一次）

**多盤口處理：** 每場賽事僅顯示 1 行（取單一主盤）。原站每場多行（不同盤口並列）的設計在本案不還原。

---

#### 頁面三：歷史賽事頁（history.html）

顯示已開賽（含進行中與已結束）的賽事彙總列表。

**球種範圍：** 美棒（MLB）、台棒（CPBL）、日棒（NPB）共 3 種。其餘 18 種球類入口顯示（外觀還原），但點進去顯示「暫無賽事」。

**資料範圍：** 已開賽的所有賽事，包含進行中的賽事。比分欄空白為正常狀態（例如半場已結束、全場尚未結束的情境）。

**全場／半場拆兩筆邏輯：**

同一場賽事在頁面顯示為兩筆獨立資料列：

| 筆次 | 標籤 | 說明 |
|---|---|---|
| 第一筆 | [全場] | 全場賭盤資料；進行中時比分可能為空 |
| 第二筆 | [半場] | 半場賭盤資料；半場結束後有比分 |

兩筆共用相同的賽事編號與開賽時間，第一欄加標籤區分。

**盤口數量：** 每場賽事每筆各顯示 1 行（單一主盤）。歷史賽事頁無 6 個盤口階段頁籤橫條（此元素為即時注單頁專屬，歷史賽事頁原站截圖無此元素）。

**帳務日期預設值：** 今日（非昨日）。

**比分欄空白處理：** 直接顯示空白，不插入「-」或其他佔位字元。

**功能按鈕：** 「重新整理」按鈕保留功能，點擊後重讀 Supabase matches 表與 tickets_aggregate 表。

**篩選下拉元件（純外觀）：** 頁面上所有篩選下拉（帳務日期選擇器、單式下拉、全場/第1頁下拉、注單種類下拉、排序/聯盟下拉）一律純外觀，點擊不觸發任何行為。

**表格欄位（棒球球種，每場賽事 2 列：[全場] + [半場]，每列單盤口）：**

| 欄位 | 顯示內容 | 資料源 |
|---|---|---|
| 編號 / 時間 | 同一場賽事全場與半場兩筆共用相同編號與開賽時間 | API matches.match_id / game_time |
| 隊伍 | 客隊名稱換行主隊名稱[主] | API matches.away_team / home_team |
| 比分 | 全場或半場比分；進行中或未結束時留空（不插入佔位字元） | API matches 賽果資料 |
| 讓分 | 讓分值 + 主家賠率/客家賠率 + 主家注單數/累計金額 / 客家注單數/累計金額 | 賠率 API + 注單彙總（業主後台填） |
| 大小 | 大小值 + 大盤賠率/小盤賠率 + 大盤注單數/累計金額 / 小盤注單數/累計金額 | 同上 |
| 獨贏 | 主家賠率/客家賠率 + 主家注單數/累計金額 / 客家注單數/累計金額 | 同上 |
| 一輸 | 棒球球種特有欄位（美棒/台棒/日棒 均含此欄）；值 + 主賠率/客賠率 + 主注單數/累計金額 / 客注單數/累計金額 | 同上 |
| 單雙 | 單賠率/雙賠率 + 單注單數/累計金額 / 雙注單數/累計金額 | 同上 |

**資料源分層：**
- 賽事資訊（編號、時間、隊伍、比分）：API matches 表（每日同步）
- 5 種盤口賠率值（讓分/大小/獨贏/一輸/單雙）：API matches 表（每日同步，含全場與半場分別的盤口資料）
- 注單數/累計金額：Supabase tickets_aggregate 表（業主後台填，重新整理按鈕觸發重讀）

---

### 4.2 編輯模式運作機制

本系統**無獨立後台介面**，所有業主操作整合在前台頁面，透過 URL hash 控制是否進入「編輯模式」。

**模式切換：**

| 網址形式 | 模式 | 業主能做的事 | 客戶端可見 |
|---|---|---|---|
| `https://demo.com/` | 純檢視模式 | 純看畫面 | 完全看不到任何編輯入口 |
| `https://demo.com/?edit=true` | 編輯模式 | 所有可改欄位變可點編輯，全域動作按鈕出現 | 業主自己用瀏覽器書籤存此網址 |

**業主使用流程：**
1. 業主把「編輯版」網址加入瀏覽器書籤（命名「Demo 編輯版」）
2. 演示前點書籤打開編輯版，inline 改數字、改跑馬燈等
3. 演示時改用「純檢視版」網址（或另一個書籤），客戶看不到任何編輯入口

**編輯模式啟用後的視覺差異：**
- 可編輯欄位 hover 時出現淡色背景提示（「這格可改」）
- 點擊欄位變輸入框，按 Enter 或滑鼠移開即時存回 Supabase
- Header 右上角出現「編輯模式中」小標示（給業主自己確認）
- 全域動作按鈕浮現於頁面右下角（匯出 / 匯入 / 同步 / 清空）

---

#### 編輯模式可編輯項目（依出現位置分類）

**A. 查詢報表頁 inline 編輯：**

| 元件 | 編輯方式 |
|---|---|
| 網站業績表 14 欄資料 | 點任一格 → 變輸入框 → 改完按 Enter 或失焦 → 存回 sites 表 |
| 「+ 新增網站」按鈕 | 表格底部，點擊新增一列空白資料 |
| 列右側垃圾桶圖示 | 點擊刪除該列 |
| 帳務日期起 / 終 | 純檢視模式也可改（已有功能），編輯模式無差異 |

**B. 即時注單頁 / 歷史賽事頁 inline 編輯：**

| 元件 | 編輯方式 |
|---|---|
| 各場賽事各盤口的「注單數 / 累計金額」 | 點任一彙總值 → 變輸入框 → 改完即時存回 tickets_aggregate 表 |
| **不可編輯**：賽事編號、時間、隊伍、賠率值（從 API 來，業主不能改） |

**C. Header 顯示設定 inline 編輯：**

| 元件 | 編輯方式 |
|---|---|
| 跑馬燈文字 | 點跑馬燈 → 彈出文字編輯框 → 儲存後即時更新並繼續滾動 |
| 右上帳號名稱（mx5144 部分） | 點該文字直接改 |
| 右上角色標籤（[子帳號]） | 點該文字直接改 |
| 右上剩餘額度數字 | 點該數字直接改 |
| 基準日期（即時注單 / 歷史賽事頁的「今日」） | 點該日期直接改 |

**D. 右下角全域動作按鈕（編輯模式才出現）：**

| 按鈕 | 功能 |
|---|---|
| 匯出 JSON | 打包所有資料（sites + tickets_aggregate + matches + 顯示設定）下載 |
| 匯入 JSON | 上傳 JSON 完整取代目前資料 |
| 立刻同步 API | 立即呼叫 SportsGameOdds 更新 matches 表 |
| 顯示同步狀態 | 顯示最後同步時間與本次同步結果 |
| 批量隨機填注單 | 一鍵為所有未填注單彙總的賽事生成隨機合理範圍值 |
| 清空注單彙總 | 僅清空 tickets_aggregate 表 |
| 清空網站業績 | 僅清空 sites 表 |
| 重置預設資料 | 清除目前資料，載入出廠預設示範資料 |

**全域動作的安全防護：**
- 危險動作（清空、重置）按下後二次確認彈窗
- 匯入 JSON 顯示資料摘要（幾筆 sites、幾筆 tickets_aggregate）後再確認

---

#### 編輯模式 vs 純檢視模式差異總表

| 比較項目 | 純檢視模式 | 編輯模式 |
|---|---|---|
| 網址 | `?` 無參數 | `?edit=true` |
| 欄位 hover 提示 | 無 | 淡色背景 |
| 欄位點擊 | 無反應（純視覺） | 變輸入框 |
| 「+ 新增網站」按鈕 | 隱藏 | 顯示 |
| 列刪除垃圾桶 | 隱藏 | 顯示 |
| 跑馬燈點擊 | 無反應 | 彈出編輯 |
| 右下全域動作按鈕 | 隱藏 | 顯示 |
| Header「編輯模式中」標示 | 隱藏 | 顯示 |
| 帳務日期起 / 終、7 個快捷按鈕 | 可改（決策延續） | 可改 |

---

#### 安全模型說明（重要邊界）

URL hash 編輯模式為**前端介面層級**的存取控制，適用於教學 Demo 情境，但有以下邊界，員工開發與業主使用均須知悉：

| URL hash 防得了 | URL hash 防不了 |
|---|---|
| 客戶在畫面上誤觸（純檢視模式下完全看不到任何編輯入口、欄位點不動、按鈕隱藏） | 客戶刻意打開瀏覽器開發者工具（DevTools），讀取 Supabase URL 與 anon key，直接從 console 呼叫 Supabase API 寫入 / 修改 / 刪除資料 |

**本案的安全模型取捨：**
- 教學 Demo 情境下，客戶在現場不會主動使用 DevTools 攻擊
- 業主自己以「編輯版書籤」管理存取，無需密碼
- **接受此邊界**——不啟用 Supabase Auth 與 RLS（資料表層級權限規則）

**若日後需求升級（業務上線、對抗惡意操作）：**
- 須加 Supabase Auth（帳號密碼登入）
- 須設定 RLS 規則（資料表層級權限）
- 編輯模式入口改為「登入後才生效」，URL hash 僅控制 UI 顯示
- 此情境屬二期擴充範圍，本案不含

---

## 五、互動範圍定義

本章節明確定義全站各元件的互動狀態，避免開發時對「純外觀」與「有功能」產生歧義。

**定義原則：** 頁面內篩選下拉一律純外觀；Header nav 切換、球種切換、功能按鈕保留互動。**例外 1：** 查詢報表頁日期欄位與 7 個快捷按鈕在純檢視模式即可改，修改結果不影響表格資料。**例外 2：** 編輯模式（URL `?edit=true`）啟用時，所有可編輯欄位（網站業績 14 欄、注單彙總值、跑馬燈、Header 顯示設定）變為可點 inline 編輯，並顯示右下角全域動作按鈕區。

### 5.1 保留互動的元件

| 元件 | 出現位置 | 互動行為 |
|---|---|---|
| Header nav 頁面切換 | 全站 Header | 點擊後切換到對應頁面（管理 / 即時注單 / 歷史賽事 / 查詢報表 / 在線會員 / Super彩 / 說明書） |
| 「即時注單」nav 入口下拉 | Header nav | 滑鼠移上後展開 21 球種選單（外觀完整 1:1 還原） |
| 球種切換（美棒 / 台棒 / 日棒） | 即時注單頁、歷史賽事頁 | 點擊切換到對應球種頁；其他 18 種球類點擊後顯示「暫無賽事」或設為不可點擊 |
| 11 秒倒數自動刷新 | 即時注單頁 | 倒數歸零後自動從 Supabase tickets_aggregate 表重讀更新畫面 |
| 「立即更新」按鈕 | 即時注單頁 | 點擊立刻從 Supabase tickets_aggregate 表重讀 |
| 「重新整理」按鈕 | 歷史賽事頁 | 點擊重讀 Supabase matches 表與 tickets_aggregate 表 |
| 「刷新」按鈕 | 查詢報表頁標題列 | 點擊重讀 Supabase sites 表 |
| 帳務日期起 / 終 輸入框 | 查詢報表頁篩選列第一排 | 前台可直接點改；修改後即時存回 Supabase sites 表對應欄位；不影響表格其他欄位數值 |
| 7 個快捷按鈕（前一日 / 今日 / 後一日 / 本週 / 上週 / 本月 / 上月） | 查詢報表頁篩選列第一排 | 點擊後自動填入日期欄位；效果等同手動改日期 |
| 編輯模式 URL hash 切換 | 全站任何頁面 | 網址結尾加 `?edit=true` 啟用編輯模式；移除參數回到純檢視模式 |
| Inline 欄位編輯 | 全站可編輯欄位（編輯模式啟用時） | 點欄位變輸入框，按 Enter 或失焦後即時存回 Supabase |
| 右下全域動作按鈕（編輯模式） | 全站右下角浮動 | 匯出/匯入 JSON、立刻同步 API、批量隨機填注單、清空、重置 |

### 5.2 純外觀元件（點擊不觸發任何行為）

| 元件 | 出現位置 | 說明 |
|---|---|---|
| 報表分類下拉 | 查詢報表頁篩選列第一排 | 純視覺展示 |
| 投注種類下拉 | 查詢報表頁篩選列第一排 | 純視覺展示 |
| 球種下拉 | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 賽程時段下拉 | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 整數下拉（數字格式） | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 帳號輸入框 | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 單號輸入框 | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 查詢按鈕 | 查詢報表頁篩選列第二排 | 純視覺展示 |
| 上一頁按鈕 | 查詢報表頁標題列 | 本頁無下鑽，此按鈕純視覺 |
| 表格藍字下注金額 | 查詢報表頁主表格 | 藍色樣式保留（視覺與原站一致），但點擊無效果（本頁無下鑽功能） |
| 「單式」下拉（盤口階段選擇） | 即時注單頁、歷史賽事頁 | 純視覺展示 |
| 「全場/第1頁」下拉 | 即時注單頁、歷史賽事頁 | 純視覺展示 |
| 「注單種類」下拉 | 即時注單頁、歷史賽事頁 | 純視覺展示 |
| 「15秒」下拉（更新頻率） | 即時注單頁 | 純視覺展示，刷新頻率固定 11 秒不可調 |
| 「帳務日期」日期選擇器 | 歷史賽事頁 | 純視覺展示，預設今日 |
| 「排序（聯盟）」下拉 | 即時注單頁、歷史賽事頁 | 純視覺展示 |
| 6 個盤口階段頁籤中的 5 個（早餐 / 走地 / 3 局 / 7 局 / 已開賽） | 即時注單頁 | 外觀 1:1 還原，點擊顯示空白或「暫無資料」；僅「單式」有功能 |
| 重設密碼入口 | 全站 Header 右上 | 純外觀，不接邏輯 |
| 登出按鈕 | 全站 Header 右上 | 純外觀，不接邏輯 |

---

## 六、外部 API 整合規格

### 6.1 API 選型

| API 服務 | 角色 | 免費方案 | 備註 |
|---|---|---|---|
| SportsGameOdds | **首選** | 有（需驗證額度） | 覆蓋 MLB / CPBL / NPB；實作前須實際驗證 CPBL 資料品質與免費配額上限 |
| The Odds API | 備案 | 500 calls/月 | MLB 強；CPBL / NPB 覆蓋待驗證 |
| Data Sports Group | 排除 | 否（企業詢價） | 過貴，超出 Demo 定位 |

> **規格書不寫死「免費方案」**：實作前須實際註冊並驗證免費額度與服務條款。若免費方案不符需求（例如 CPBL 資料未覆蓋），備案為付費方案（USD $50～200/月），費用需另議。

### 6.2 API 呼叫與同步架構

**解耦原則：API 同步頻率與前台刷新頻率完全分離。**

| 資料類型 | 更新方式 | 頻率 | 預估 call 數 |
|---|---|---|---|
| 賽事 + 盤口 | 背景排程呼叫 SportsGameOdds API → 存入 Supabase matches 表 | 每日 1～2 次 | 30～60 次/月（遠低於 500 calls/月 免費上限） |
| 注單彙總資料 | 後台填入賽事盤口彙總值 → 存入 Supabase tickets_aggregate 表 | 業主操作時 | 不呼叫外部 API |
| 前台即時注單頁刷新 | 每 11 秒從 Supabase tickets_aggregate 表讀取 | 11 秒/次 | 不呼叫外部 API |
| 查詢報表頁刷新 | 業主點「刷新」從 Supabase sites 表讀取 | 手動觸發 | 不呼叫外部 API |

### 6.3 API 取回資料的使用方式

- 取回賽事清單後，依球種分類存入 Supabase matches 表
- 每場賽事固定取一家 sportsbook 為主盤（建議 Pinnacle，或 API 預設首家）
- 每場賽事只儲存一筆記錄（即一個主盤），不儲存多家賠率
- 賽事狀態（未開賽 / 進行中 / 已完成）同步更新

### 6.4 降級備案

若 API 服務暫時不可用：
- 前台即時注單頁與歷史賽事頁顯示「賽事資料更新中」提示
- 已存在 Supabase 的注單與賽事資料仍可正常顯示，不影響查詢報表頁功能
- 查詢報表頁完全不依賴 API，業主手填資料不受 API 狀態影響

---

## 七、Supabase 雲端服務規格

| 項目 | 規格 |
|---|---|
| 方案 | Free Tier（免費） |
| 資料庫 | PostgreSQL |
| 即時同步 | Supabase Realtime：編輯模式中業主修改後前台 1 秒內更新 |
| 存取控制 | 不啟用 Auth；編輯模式由前端 URL hash 控制（`?edit=true`）；客戶端純網址無法觸發任何寫入操作 |
| 帳號歸屬 | 建議開立在業主名下，開發方協助設定 |
| 免費方案限制 | 500MB 資料庫、月流量 5GB、同時連線 200（示範用途綽綽有餘） |
| 超出免費限制 | 業主自行決定是否升級 Pro（約 USD $25/月）；費用不在本案範圍 |

---

## 八、瀏覽器與裝置支援

| 項目 | 規格 |
|---|---|
| 支援瀏覽器 | Google Chrome（最新版）、Microsoft Edge（最新版） |
| 最低螢幕寬度 | 1170px |
| 行動裝置 | 不支援 |
| 平板裝置 | 不保證正常顯示 |

---

## 九、排除項目

1. **代理樹架構**：v2.0 已整套移除，查詢報表頁改為純手動建單，不再有代理樹資料結構、不再有代理鏈欄位、不再有 7 層結果自動計算引擎（業主手填結果數值）
2. **注單驅動報表推導**：v2.0 查詢報表頁不從注單計算任何欄位，所有欄位由業主直接填入 sites 表
3. **RG 真人（BillDG）模組**：同一頁面結構但獨立資料集，歸入二期範圍
4. **上層管理功能**：開立子帳號、調整成數、設定層級額度、會員帳號管理
5. **RWD 響應式設計**：原站 `min-width: 1170px`，桌機限定；硬做 RWD 會破壞版面還原精度
6. **真實結算引擎**：Demo 用途，不需實際對帳
7. **金流串接**：不接儲值、出款、支付
8. **帳號申請系統**：不開放第三方自行註冊
9. **多語系切換**：超出 Demo 定位
10. **原站品牌資產（Logo、商標）**：一律不沿用，替換為中性占位或業主自訂品牌
11. **SEO 優化**：非 Demo 核心需求
12. **後台操作記錄（Audit Log）**：非 Demo 核心需求
13. **重設密碼 / 登出功能實作**：純外觀，不接邏輯
14. **串接實際博弈商一手盤口**：無博弈執照、無授權，技術上無法取得
15. **爬取中文賽事網站**：法律灰色地帶，維運脆弱（對方改版即壞）
16. **即時注單頁除「單式」以外的 5 個盤口階段**：外觀還原，無功能
17. **即時注單頁除美棒、台棒、日棒以外的 18 種球類**：入口圖示還原，無資料
18. **獨立後台介面（admin.html）**：v2.0 採全前台 inline 編輯架構，所有業主操作整合於前台編輯模式（URL `?edit=true` 觸發），不再有獨立後台頁面
19. **使用者帳號密碼登入機制**：編輯模式由 URL hash 控制，不需 Auth；業主自行管理「編輯版書籤」即可控制存取

---

## 十、交付物清單

| 編號 | 交付物 | 說明 |
|---|---|---|
| 1 | 前台原始碼 | index.html（查詢報表頁）、live-bets.html（即時注單）、history.html（歷史賽事）、CSS、JS |
| 2 | 編輯模式邏輯 | URL hash 偵測切換、inline 欄位編輯、右下全域動作按鈕區、二次確認彈窗 |
| 3 | Supabase 設定說明 | 資料表結構（sites、tickets_aggregate、matches 三表）、Realtime 設定文件 |
| 4 | API 同步排程說明 | SportsGameOdds API 同步腳本設定與每日排程配置說明 |
| 5 | 預設 Demo 資料包 | 可直接匯入的 JSON，含顯示設定、預設網站業績列（sites 表範例資料）、預填賽事盤口彙總值（tickets_aggregate 表，多球種）|
| 6 | 使用說明文件 | 編輯模式操作手冊（書籤設定、inline 編輯、批量隨機填、備份還原、日期設定） |
| 7 | 部署說明 | 上架至 Netlify 或業主自有主機的步驟 |

---

## 十一、驗收標準

| 編號 | 驗收項目 |
|---|---|
| V-01 | 查詢報表頁外觀與參考站一致（Header、顏色 `#dc3545`、字型、表格樣式、跑馬燈） |
| V-02 | 查詢報表頁主表格正確顯示業主建立的所有 sites 表資料列，欄位對應正確 |
| V-03 | 合計列正確加總 11 個數字欄位；備註與成數欄合計列留空 |
| V-04 | 正值欄位顯示綠色，負值欄位顯示紅色（會員結果至大總監結果 7 欄均套用） |
| V-05 | 查詢報表頁日期起 / 終欄位前台可直接點改，7 個快捷按鈕可自動填日期；修改後即時存回 Supabase |
| V-06 | 日期修改後表格其他欄位數值不受影響（日期與資料完全分離） |
| V-07 | Header nav 切換正常（點擊各頁面入口可切換頁面） |
| V-08 | URL hash 編輯模式正常切換：純網址無 `?edit=true` 時所有編輯入口隱藏；加上 `?edit=true` 時可編輯欄位 hover 出現提示、右下全域動作按鈕浮現、Header 顯示「編輯模式中」標示 |
| V-09 | 編輯模式中查詢報表頁可 inline 點欄位變輸入框、改完即時存回 sites 表；可新增列、可刪除列 |
| V-10 | 新增 / 修改 sites 表後，前台查詢報表頁 1 秒內自動更新（Supabase Realtime） |
| V-11 | 編輯模式中右下角「批量隨機填注單」按鈕一鍵生成所有未填賽事的彙總值，正確存入 tickets_aggregate 表 |
| V-12 | 編輯模式中即時注單頁 / 歷史賽事頁可 inline 點注單彙總值改、即時存回 tickets_aggregate 表 |
| V-13 | 編輯模式中右下「立刻同步 API」按鈕成功呼叫 SportsGameOdds 並更新 matches 表；顯示最後同步時間正確 |
| V-14 | 編輯模式中可 inline 改跑馬燈文字、Header 帳號名稱、剩餘額度、基準日期，前台即時更新 |
| V-15 | 編輯模式中右下全域按鈕（匯出 / 匯入 JSON、清空注單彙總、清空網站業績、重置預設資料）正常運作；危險動作有二次確認彈窗 |
| V-16 | 即時注單頁「單式」頁籤正確顯示 tickets 表注單，6 個頁籤外觀完整還原 |
| V-17 | 即時注單頁 11 秒倒數自動刷新正常，「立即更新」按鈕正常 |
| V-18 | SportsGameOdds API 賽事資料可成功同步進 Supabase matches 表 |
| V-19 | 批量生單從 matches 表正確取得賽事，注單資料與賽事關聯正確 |
| V-20 | 歷史賽事頁同一場賽事正確拆成 [全場] 與 [半場] 兩筆資料，編號與開賽時間相同，標籤正確顯示 |
| V-21 | 歷史賽事頁比分欄空白時直接留空（不插入「-」或其他佔位字元）；帳務日期預設為「今日」 |
| V-22 | 歷史賽事頁「重新整理」按鈕正常重讀 Supabase 資料；頁面內所有篩選下拉點擊後無任何行為（純外觀） |
| V-23 | Chrome、Edge 最新版，1170px 以上螢幕寬度，三個前台頁面版面無破版 |

---

## 十二、工期估算

| 階段 | 工作內容 | 預估工作天 |
|---|---|---|
| 環境建置 | Supabase 設定、Auth、三張資料表設計（sites / tickets / matches） | 1 天 |
| API 整合 | SportsGameOdds 串接、同步排程、matches 表建置 | 1～2 天 |
| 查詢報表頁 | 篩選列外觀（含日期互動邏輯）、主表格、合計列、正負值顏色、Realtime 訂閱 | 2～3 天 |
| 即時注單頁 | 6 個頁籤外觀、單式頁功能、11 秒倒數刷新、資料來源串接 | 2～3 天 |
| 歷史賽事頁 | 全場 / 半場拆兩筆邏輯、棒球欄位（含一輸欄）、重新整理按鈕、篩選下拉純外觀 | 1～2 天 |
| 編輯模式機制 | URL hash 切換偵測、inline 欄位編輯通用元件、右下全域動作按鈕區、危險動作二次確認彈窗 | 1.5～2.5 天 |
| 資料計算邏輯 | 合計列即時加總、正負值判斷、日期存回邏輯 | 0.5～1 天 |
| 設計還原 | 比對參考站，調整視覺細節（色碼、字型、邊距） | 1 天 |
| 測試 + 修正 | 功能驗收、跨瀏覽器測試、V-01～V-23 逐項確認 | 1～2 天 |
| **合計** | | **11～17 個工作天** |

> 工期以雙方確認規格書後起算，不含業主提供品牌素材（Logo、品牌名稱）的等待時間。

---

## 十三、授權與聲明

1. **原站品牌不得使用**：不得沿用原參考站品牌名稱、Logo、商標。
2. **非實際營運聲明**：本系統不連接任何真實博弈平台，不執行實際下注、結算、金流操作。不得作為向第三方宣稱實際戰績或招攬會員的工具。
3. **API 資料聲明**：本系統透過公開授權的體育賠率聚合 API（非一手盤口商資料）取得賽事資料，用於展示目的，不作為實際投注依據。
4. **資料安全**：業主負責妥善保管 Supabase 帳號密碼及後台管理員帳號。
5. **原始碼歸屬**：原始碼於交付並完成付款後歸業主所有，開發方不保留副本。
6. **保固期**：交付後【待填入】個月內，因開發方程式錯誤產生的 Bug 免費修正。保固期後如需修改或新增功能，另行議價。

---

## 附錄

### 附錄 A：21 種球種清單

| ID | 名稱 | ID | 名稱 |
|---|---|---|---|
| 3 | 籃球 | 83 | 彩球 |
| 16 | 其它籃球 | 84 | 指數 |
| 4 | 美棒 | 72 | 賽馬賽狗 |
| 12 | 日棒 | 85 | 電競 |
| 14 | 韓棒 | 22 | 羽毛球 |
| 11 | 台棒 | 23 | 排球 |
| 13 | 其它棒球 | 24 | 撞球 |
| 1 | 足球 | 21 | 乒乓球 |
| 55 | 網球 | 31 | 冠軍聯賽 |
| 82 | 冰球 | 98 | 超吉8 |
| 5 | 美足 | | |

### 附錄 B：即時注單頁與歷史賽事頁支援球種

| 球種 | 英文代碼 | API 識別碼（SportsGameOdds） | 說明 |
|---|---|---|---|
| 美棒 | MLB | baseball_mlb | 美國職棒大聯盟 |
| 台棒 | CPBL | baseball_cpbl | 中華職棒 |
| 日棒 | NPB | baseball_npb | 日本職棒 |

> 實作前須驗證 SportsGameOdds 對 CPBL 的實際覆蓋狀況（賽季期間是否有穩定資料）。

### 附錄 C：設計 Token（從原站擷取）

| Token | 值 | 使用位置 |
|---|---|---|
| 主色 | `#dc3545` | Header、按鈕、表頭、分隔線 |
| 紅色 hover | `#bb2d3b` / `#b02a37` | 按鈕 hover 狀態 |
| Header 漸層 | `linear-gradient(90deg, #dc3545 0%, #ae6168 100%)` | Header 背景 |
| 字型 | `"Helvetica", "Arial","LiHei Pro","黑體-繁","微軟正黑體", sans-serif` | 全站 |
| 基準字級 | 10pt | 全站 |
| 連結藍 | `blue` / hover `#07198d` | 超連結、藍色文字 |
| 賠率藍 | `#1e81b0` | 賠率數字 |
| 讓分橘 | `#e28743` | 讓分數字、表頭橘底 |
| 勝贏綠 | `green` / `#23ba1e` | 正值結果 |
| 負值紅 | `#dc3545` | 負值結果 |
| Min-width | 1170px | 全站最小寬度 |

---

*本規格書版本 v2.0，如有任何修改應由雙方確認後以書面（含電子郵件）記錄，並更新版本號。*
